mixin gratitudeSlide(img, alt, name, text)
  +e.slide
    +e.img
      img(src= "img/thanksletters/" + img alt= alt title= alt)
      +e.A.link(href='#')
      +e.P.name #{name}
      +e.P.text #{text}
    +e.modal
      img(src= "img/thanksletters/" + img alt= alt title= alt)
  //- можно еще задать width/height, но т.к. изображения в контейнерах, все равно растянутся
